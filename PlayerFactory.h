#pragma once

#include "Player.h"
#include "DefaultMovement.h"

class PlayerFactory {
public:
	//static Player createPlayer() {
	//	return Player(std::make_unique<DefaultMovement>(500));
	//}
};
